{
  "buildCommand": "pnpm --filter @workspace/home-tracker run build && mv artifacts/home-tracker/dist dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
